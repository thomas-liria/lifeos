# PROMPTS_LOG.md

One-line summary of each prompt and what was built.

---

| # | Prompt summary | What was built |
|---|----------------|----------------|
| 1 | Create PROJECT_CONTEXT.md living document | `PROJECT_CONTEXT.md` written to Desktop root with full app spec |
| 2 | Scaffold project structure, shell layout, CSS system | Next.js app in `/lifeos`, full CSS variable colour system, TopBar + SmartInputBar + BottomTabBar shell, 4 placeholder screens, complete module/lib/hooks/types folder structure |
| 3 | Build Home screen content | GreetingCard (time-aware, date, localStorage task count), FocusCard (3 hardcoded urgent tasks), WorkspaceChips (4 chips, School locked), HabitsStrip (3 toggleable habit pills) |
| 4 | Build Routine screen | ProgressRing (SVG, animated, completion pulse), useRoutine hook (localStorage, date-keyed resets), RoutineItem (circular checkbox, drag handle, long-press delete), RoutineScreen (dnd-kit sortable, inline add form, resets-at-midnight footer) |
| 5 | Build Tasks screen | useTasks hook (localStorage, seed data, 7 placeholder tasks), TaskItem (circular checkbox, urgency pill, due date formatting), AddTaskForm (inline, urgency toggle, due date chips + native date picker), TasksScreen (3 workspace tabs, 4 filters, completed sink to bottom, floating FAB, ?workspace= URL param routing) |
| 6 | Wire up Smart Capture bar with Claude API | @anthropic-ai/sdk installed, /api/capture route (POST, claude-sonnet-4-20250514, cached system prompt, JSON extraction), CaptureCard (slide-up, workspace/urgency/due badges, unclear workspace selector, low-confidence note, save to lifeos_tasks localStorage), SmartInputBar updated (Enter/Send, breathing loading animation, inline error state) |
| 7 | Build Gym Tracker (Log screen) | recharts installed, LogScreen (Gym/Daily Log tabs), GymTab orchestrator, SessionHeaderCard (next session in rotation, Start Session), LastSessionCard (placeholder + real data), ActiveSessionLogger (elapsed timer, exercise cards, set rows with weight/reps/done checkbox, autocomplete from 22-exercise preset list, Add/Finish), ProgressChart (collapsible recharts LineChart, bench press placeholder data, mounted guard), StreakCard (this-week progress bar + dot indicators), useGym hook (localStorage lifeos_gym_sessions, rotation logic), types.ts |
| 8 | Build Settings screen | Full-screen /settings route via gear icon in TopBar (TopBar updated: back arrow on /settings, gear+ThemeToggle hidden). providers.tsx: enableSystem added for 3-way theme. useRoutine: updateItem added. useSettings hook (lifeos_settings localStorage, SettingsData type, 6 deep-update helpers). 7 sections: (1) Routine — dnd-kit sortable items, inline tap-to-edit, delete, time-estimate toggle; (2) Habits — gym days/rotation reorder, red light days+duration steppers, supplements toggle, add custom habit; (3) Workspaces — rename inline, School Activate form with timetable textarea; (4) Notifications — Off/Gentle/Active segmented control + per-workspace toggles; (5) Integrations — 5 rows, "coming soon" on connect tap; (6) Appearance — Light/Dark/System 3-way segmented (next-themes); (7) Data — export JSON blob download + clear all with confirmation. Reusable SettingsSection wrapper, ToggleSwitch, Stepper primitives. "Saved" toast with saved-fade CSS animation after every change. |
| 10 | Polish pass — spacing, mobile feel, animations, dark mode, empty states | (1) globals.css: --text-tertiary token (light+dark), html scroll-behavior:smooth, * -webkit-tap-highlight-color:transparent, fade-up/tab-in/scroll-touch CSS classes; (2) BottomTabBar: icons 20→24px, labels 10→11px, min-h-[64px] on each tab link; (3) SmartInputBar: placeholder "What's on your mind…", input text-[15px], send arrow refined; (4) HomeScreen: gap-5→gap-6, each card wrapped in fade-in-card div with 60ms stagger delays; (5) WorkspaceChips: h-14 (56px) chips, count displayed as text-[18px] bold with "task/tasks" at text-[10px], active:scale-[0.97] press feel, scroll-touch; (6) FocusCard/HabitsStrip/SmartPlanCard: section labels unified to text-[11px] tracking-[0.08em] text-text-tertiary; (7) SmartPlanCard: row padding py-3.5, chevron text-foreground/35→/40 with group-hover; (8) HabitsStrip: min-h-[44px] pills, scroll-touch, active:scale-[0.97]; (9) TasksScreen: filter chips py-2 min-h-[36px], task list/empty state keyed for tab-fade animation, Inbox icon empty state; (10) LogScreen: tab content keyed for tab-fade, CalendarDays icon placeholder; (11) All loading states: icon + muted text (Inbox, ListChecks, Dumbbell, Settings2, Leaf icons replacing plain "Loading…"); (12) TypeScript: 0 errors; build: clean. |
| 9 | Smart Daily Scheduler — wire home screen to live data | Central useHomeData hook (reads lifeos_tasks + lifeos_gym_sessions + lifeos_settings + lifeos_habits_YYYY-MM-DD). All components refactored to receive props from HomeScreen.tsx wrapper. (1) GreetingCard: real count of today+overdue incomplete tasks; (2) FocusCard: top-3 tasks sorted by priority (overdue-urgent → overdue → today-urgent → today → upcoming), loading skeletons; (3) WorkspaceChips: live incomplete-task counts per workspace, workspace names from settings; (4) HabitsStrip: gym pill shown if no session today or yesterday AND week count < target (from lifeos_gym_sessions), red light shown if not done today AND week count < target (from Mon–yesterday habit records), supplements shown if reminder on — completion persists in lifeos_habits_YYYY-MM-DD, pills fixed at load time so they don't vanish mid-session; (5) SmartPlanCard (new): ranked "Today's focus" list — overdue tasks → urgent today → gym if scheduled → normal today → "Complete your morning routine" — each row has urgency dot + workspace badge + tag pill + chevron, taps navigate to relevant tab. |
| 11 | Google Calendar OAuth integration | OAuth flow: GET /api/integrations/google-calendar/connect (redirects to Google consent), GET /api/integrations/google-calendar/callback (exchanges code for tokens, encodes as base64 JSON in URL hash, redirects to /auth/gcal-success), /auth/gcal-success client page (decodes hash, writes to localStorage "lifeos_integration_google_calendar" as {accessToken, refreshToken, expiresAt, connectedAt}, navigates to /home). Token refresh utility: src/lib/integrations/google-calendar/client.ts (getValidToken — checks expiry with 60s buffer, calls Google /token with refresh_token grant, returns updatedTokens). Events API: GET /api/integrations/google-calendar/events (accepts tokens via Authorization + X-Refresh-Token + X-Token-Expires-At headers, auto-refreshes, fetches primary calendar next 7 days, filters cancelled + declined, returns {events, updatedTokens}). Shared types: src/lib/integrations/google-calendar/types.ts (CalendarTokens, CalendarEvent, UpdatedToken). Home screen: useHomeData extended — on mount reads gcal token from localStorage, fetches events async, updates lastSyncedAt, sets calGymBlocked if any event today falls in 6–9am or 5–8pm gym windows, returns upcomingEvents (next 2 within 48h). UpcomingEventsCard (new) — hidden when no events, shows calendar icon badge + title + day/time, sits below HabitsStrip. Settings/Integrations: GoogleCalendarRow fully replaced — not-connected shows "Connect" button (navigates to /api/.../connect); connected shows green "Connected" + last synced time + "Sync now" (refetches + updates lastSyncedAt) + "Disconnect" (clears localStorage). All other integrations remain "coming soon" placeholders. Build: 0 TS errors, all 4 new routes confirmed in build output. |
| 12 | Jobber integration — WeedGuys workspace (read-only) | OAuth flow: GET /api/integrations/jobber/connect (redirects to Jobber consent), GET /api/integrations/jobber/callback (exchanges code, encodes tokens as base64 in URL hash, redirects to /auth/jobber-success), /auth/jobber-success client page (decodes hash, writes to localStorage "lifeos_integration_jobber", navigates to /tasks?workspace=weedguys). Token refresh: src/lib/integrations/jobber/client.ts (getValidToken — 60s buffer, refresh_token grant against https://api.getjobber.com/api/oauth/token). GraphQL data route: GET /api/integrations/jobber/data (fetches invoices[PAST_DUE], jobs[SCHEDULED/IN_PROGRESS], requests[PENDING] via single GraphQL query, parses to JobberInvoice/JobberJob/JobberRequest types, computes outstandingBalance, returns {overdueInvoices, upcomingJobs, recentRequests, outstandingBalance, updatedTokens}). Shared types: src/lib/integrations/jobber/types.ts (JobberTokens, JobberInvoice, JobberJob, JobberRequest, JobberData, UpdatedJobberToken). useJobberData hook: returns MOCK_DATA immediately if no token (with isMock:true banner), fetches live data and persists refreshed tokens when connected, exposes {data, loading, error, refetch}. JobberPanel component: outstanding balance card (urgent colour when overdue invoices exist), overdue invoices list with daysOverdue chip + amount, upcoming jobs list, pending requests list, mock-data banner. TasksScreen: imports JobberPanel, renders it above task list when activeWs === "weedguys". useHomeData: fetches Jobber data on mount (parallel to calendar), surfaces overdue invoices as urgent WeedGuys plan item ("N overdue WeedGuys invoices — $X outstanding → /tasks?workspace=weedguys"). IntegrationsSection: JobberRow added (mirrors GoogleCalendarRow pattern — connect/connected/disconnect/sync-now/sync-error states), removed Jobber from PlaceholderRow list. Env vars: JOBBER_CLIENT_ID + JOBBER_CLIENT_SECRET already in .env.local. Build: 0 TS errors. |
| 14 | Supabase migration + AI assistant + Calendar view | (1) Supabase: @supabase/supabase-js installed; src/lib/supabase/client.ts (anon key, browser) + server.ts (service role, server-only); supabase/schema.sql (9 tables: tasks, routine_items, routine_completions, gym_sessions, gym_exercises, gym_sets, habit_completions, ai_messages, user_settings — all with RLS allow-all-anon policies + indexes); useTasks migrated to Supabase primary with localStorage fallback + one-time migration on first load. (2) AI assistant: /api/assistant route (claude-opus-4-7, tool use: add_task/complete_task/navigate, full context injected — incomplete tasks, today habits, gym sessions, timestamp, previous 30 messages); AssistantButton (floating bot bubble fixed bottom-right above tab bar); AssistantPanel (slide-up 85dvh chat UI, message history, typing dots, clear button, dismiss by backdrop tap or X); useAssistant hook (loads history from ai_messages, sendMessage, clearHistory); replaces SmartInputBar in shell layout. (3) Calendar: /calendar page + CalendarScreen with WeekView (Mon–Sun columns with event coloured blocks, task chips, habit/gym dot indicators, prev/next/today nav, tap-to-select day) and MonthView (grid with green=habits/amber=tasks/blue=event/grey=gym dots, tap day for detail); DayDetail bottom sheet (events, tasks due, habit+gym badges); BottomTabBar updated to 5 tabs: Home/Tasks/Routine/Calendar/Log. Build: 0 errors, 0 TS errors. Deployed. |
| 13 | Vercel deployment | Git repo initialised (was already on main branch with 1 existing commit). .gitignore updated: added .claude/ exclusion (all other sensitive files were already covered by .env*, node_modules, .next/, .DS_Store). Full deployment commit: 84 files, "Initial LifeOS deployment". Deployment instructions provided: gh repo create lifeos --public --source=. --remote=origin --push, then vercel --prod. 4 environment variables identified for Vercel dashboard (ANTHROPIC_API_KEY, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, JOBBER_CLIENT_ID, JOBBER_CLIENT_SECRET). Post-deploy checklist: update Google Cloud Console OAuth redirect URI to https://lifeos.vercel.app/api/integrations/google-calendar/callback; Jobber callback URL already set to https://lifeos.vercel.app/api/integrations/jobber/callback. |
| 15 | Fix 2 — Jobber data accuracy | Split single combined GraphQL query into 3 isolated queries (invoices, jobs, requests) via runQuery helper so one schema error doesn't fail all three. Added full raw-response logging per query for debugging. Updated field names: `total` for invoices, `scheduledStart`/`scheduledEnd` for jobs. Date-range variables: jobs filtered today→weekEnd, requests filtered sevenDaysAgo. Outstanding balance = ALL unpaid invoices (not just overdue subset). API response now includes `syncedAt` epoch, `unpaidCount`, and `schemaWarnings` array for non-fatal field errors. JobberPanel rewritten: last-synced timestamp + inline Refresh link, schema warnings banner, unpaidCount shown alongside overdue count. |
